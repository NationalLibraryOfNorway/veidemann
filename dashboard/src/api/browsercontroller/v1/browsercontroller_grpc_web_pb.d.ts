import * as grpcWeb from 'grpc-web';

import * as browsercontroller_v1_browsercontroller_pb from '../../browsercontroller/v1/browsercontroller_pb'; // proto import: "browsercontroller/v1/browsercontroller.proto"


export class BrowserControllerClient {
  constructor (hostname: string,
               credentials?: null | { [index: string]: string; },
               options?: null | { [index: string]: any; });

}

export class BrowserControllerPromiseClient {
  constructor (hostname: string,
               credentials?: null | { [index: string]: string; },
               options?: null | { [index: string]: any; });

}

