apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - service.yaml
  - deployment.yaml

configMapGenerator:
  - name: controller-envoy
    files:
      - envoy.yaml
  - name: controller
    files:
      - log4j2.xml
