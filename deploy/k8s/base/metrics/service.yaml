apiVersion: v1
kind: Service
metadata:
  name: metrics
  labels:
    app.kubernetes.io/name: metrics
    metrics: prometheus
spec:
  type: ClusterIP
  ports:
    - name: http-metrics
      port: 9001
      targetPort: http-metrics
  selector:
    app.kubernetes.io/name: metrics

