apiVersion: apps/v1
kind: Deployment
metadata:
  name: rethinkdb-admin
  labels:
    app.kubernetes.io/name: rethinkdb
    app.kubernetes.io/component: dashboard
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: rethinkdb
      app.kubernetes.io/component: dashboard
  template:
    metadata:
      name: rethinkdb-admin
      labels:
        app.kubernetes.io/name: rethinkdb
        app.kubernetes.io/component: dashboard
    spec:
      containers:
        - name: rethinkdb
          image: ghcr.io/nationallibraryofnorway/veidemann/rethinkdb:0.4.4
          resources:
            requests:
              cpu: 100m
              memory: 500Mi
            limits:
              memory: 1Gi
          ports:
            - name: cluster
              containerPort: 29015
            - name: http-admin
              containerPort: 8080
          args:
            - --bind
            - $(POD_IP)
            - --no-update-check
          env:
            - name: PROXY
              value: "true"
            - name: RETHINKDB_PASSWORD
              valueFrom:
                secretKeyRef:
                  key: DB_PASSWORD
                  name: rethinkdb-env
                  optional: false
            - name: POD_IP
              valueFrom:
                fieldRef:
                  apiVersion: v1
                  fieldPath: status.podIP
            - name: POD_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
          startupProbe:
            exec:
              command:
                - /rethinkdb-probe
            failureThreshold: 10
            periodSeconds: 10
            timeoutSeconds: 5
          livenessProbe:
            exec:
              command:
                - /rethinkdb-probe
            timeoutSeconds: 5
          readinessProbe:
            exec:
              command:
                - /rethinkdb-probe
            timeoutSeconds: 5
