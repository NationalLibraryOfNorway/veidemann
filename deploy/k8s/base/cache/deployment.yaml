apiVersion: apps/v1
kind: Deployment
metadata:
  name: cache
  labels:
    app.kubernetes.io/name: cache
    app.kubernetes.io/component: cache-balancer
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: cache
      app.kubernetes.io/component: cache-balancer
  template:
    metadata:
      labels:
        app.kubernetes.io/name: cache
        app.kubernetes.io/component: cache-balancer
      annotations:
        config.linkerd.io/opaque-ports: "3128"
    spec:
      serviceAccountName: cache
      securityContext:
        fsGroup: 13
      initContainers:
        - name: wait-for-cache
          image: busybox:1.36
          env:
            - name: POD_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
          command:
            - sh
            - -c
            - |
              host="cache-0.cache.${POD_NAMESPACE}.svc.cluster.local"
              echo "Waiting for ${host}..."
              until nc -z -w 2 "${host}" 3128; do
                sleep 2
              done
              echo "cache-0 is reachable."
      containers:
        - name: cache
          image: ghcr.io/nationallibraryofnorway/veidemann/cache:0.9.2
          args:
            - -b # be a balancer
          ports:
            - name: http
              containerPort: 3128
              protocol: TCP
          env:
            - name: DNS_SERVERS
              value: dns-resolver
            - name: SERVICE_NAME
              value: cache
            - name: NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
          resources:
            requests:
              memory: 500Mi
              cpu: 100m
            limits:
              memory: 500Mi
        - name: cache-exporter
          image: docker.io/boynux/squid-exporter:v1.13.0
          ports:
            - name: http-metrics
              containerPort: 9301
              protocol: TCP
          args:
            - "-listen"
            - "0.0.0.0:9301"
