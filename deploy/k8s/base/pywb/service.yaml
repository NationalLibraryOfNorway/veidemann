apiVersion: v1
kind: Service
metadata:
  name: pywb
  labels:
    app.kubernetes.io/name: pywb
    app.kubernetes.io/component: pywb
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8080
      protocol: TCP
      targetPort: http
  selector:
    app.kubernetes.io/name: pywb
    app.kubernetes.io/component: pywb
