apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component

resources:
  - pvc.yaml
  - backup-configmap.yaml
  - uploader-scripts-configmap.yaml
  - uploader-cronjob.yaml

patches:
  - path: statefulset.yaml