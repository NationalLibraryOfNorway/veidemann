apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../../base/harvester

patches:
  - path: debug-patch.yaml
    target:
      kind: Deployment
      name: harvester

# configMapGenerator:
#   - name: chromium-policy
#     files:
#       - harvester-policy.json
