apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: default

resources:
  - ../../../base/cache
  - cache-cert.yaml

patches:
  - path: statefulset.yaml
  - path: deployment.yaml

configMapGenerator:
  - name: cache-conf
    files:
      - parent.conf
      - children.conf
