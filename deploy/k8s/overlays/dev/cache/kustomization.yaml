apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: default

resources:
  - ../../../base/cache
  - cache-cert.yaml

patches:
  - path: cache-statefulset.yaml

configMapGenerator:
  - name: cache-conf
    files:
      - cache.conf
