# Configure squid cache directory
cache_dir aufs /var/spool/squid 200 8 16 # Size: 200MB, First level dirs: 16, Second level dirs: 256

# Logging configuration
# ---------------------
logformat veidemann %ts.%03tu %>a %Ss/%03>Hs %<st %rm %ru %Sh/%<a
access_log daemon:stderr veidemann
