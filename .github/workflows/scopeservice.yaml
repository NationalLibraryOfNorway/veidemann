name: Scope Service

on:
  push:
    branches:
      - 'main'
    tags:
      - 'scopeservice/*'
    paths:
      - 'scopeservice/**'

jobs:

  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - uses: ./.github/actions/golang-test
        with:
          working_directory: scopeservice

  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - uses: ./.github/actions/golang-lint
        with:
          working_directory: scopeservice

  build:
    needs: [test, lint]
    uses: ./.github/workflows/build-and-publish.yaml
    with:
      PATH: scopeservice
      IMAGE_NAME: nettarkivet/veidemann-scopeservice
    secrets:
      HTTPS_PROXY: ${{ secrets.HTTPS_PROXY }}
      HTTP_PROXY: ${{ secrets.HTTP_PROXY }}
      NO_PROXY: ${{ secrets.NO_PROXY }}
      VAULT_URL: ${{ secrets.NB_VAULT_URL }}
      VAULT_SECRET_PATH: ${{ secrets.VAULT_SECRET_PATH }}
      VAULT_ROLE_ID: ${{ secrets.VAULT_ROLE_ID }}
      VAULT_SECRET_ID: ${{ secrets.VAULT_SECRET_ID }}
