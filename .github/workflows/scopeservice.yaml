name: Scope Service

on:
  push:
    branches:
      - 'main'
    tags:
      - 'scopeservice/*'
    paths:
      - 'scopeservice/**'
  workflow_dispatch:

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: ./.github/actions/golang-test
        with:
          working-directory: scopeservice
      - uses: ./.github/actions/golang-lint
        with:
          working-directory: scopeservice

  build:
    needs: test
    uses: ./.github/workflows/build-and-publish.yaml
    with:
      PATH: scopeservice
