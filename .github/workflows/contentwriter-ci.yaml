name: Content Writer CI

on:
  push:
    branches:
      - 'main'
    paths:
      - 'contentwriter/**'
  pull_request:
    paths:
      - 'contentwriter/**'
  workflow_dispatch:

jobs:
  ci:
    uses: ./.github/workflows/golang-ci.yaml
    with:
      working-directory: contentwriter
