name: Scope Service CI

on:
  push:
    branches:
      - 'main'
    paths:
      - 'scopeservice/**'
  pull_request:
    paths:
      - 'scopeservice/**'
  workflow_dispatch:

jobs:
  ci:
    uses: ./.github/workflows/golang-ci.yaml
    with:
      working-directory: scopeservice
