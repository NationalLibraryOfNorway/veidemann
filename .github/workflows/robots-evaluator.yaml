name: Robots(.txt) Evaluator

on:
  push:
    branches:
      - 'main'
    tags:
      - 'robots-evaluator/*'
    paths:
      - 'robots-evaluator/**'
  workflow_dispatch:

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: ./.github/actions/golang-test
        with:
          working-directory: robots-evaluator
      - uses: ./.github/actions/golang-lint
        with:
          working-directory: robots-evaluator

  build:
    needs: test
    permissions:
      contents: read
      packages: write
    uses: ./.github/workflows/build-and-publish.yaml
    with:
      PATH: robots-evaluator
