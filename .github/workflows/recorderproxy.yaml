name: Recorder Proxy

on:
  push:
    branches:
      - 'main'
    tags:
      - 'recorderproxy/*'
    paths:
      - 'recorderproxy/**'
  workflow_dispatch:

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: ./.github/actions/golang-test
        with:
          working-directory: recorderproxy
      - uses: ./.github/actions/golang-lint
        with:
          working-directory: recorderproxy

  build:
    needs: test
    permissions:
      contents: read
      packages: write
    uses: ./.github/workflows/build-and-publish.yaml
    with:
      PATH: recorderproxy
