name: Controller CI

on:
  push:
    branches:
      - 'main'
    paths:
      - 'controller/**'
  pull_request:
    paths:
      - 'controller/**'
  workflow_dispatch:

jobs:
  ci:
    uses: ./.github/workflows/java-ci.yaml
    with:
      subproject: controller
