name: veidemannctl CI

on:
  push:
    branches:
      - 'main'
    paths:
      - 'ctl/**'
  pull_request:
    paths:
      - 'ctl/**'
  workflow_dispatch:

jobs:
  ci:
    uses: ./.github/workflows/golang-ci.yaml
    with:
      working-directory: ctl
