name: First Article Inspection CI

on:
  push:
    branches:
      - 'main'
    paths:
      - 'fai/**'
  pull_request:
    paths:
      - 'fai/**'
  workflow_dispatch:

jobs:
  ci:
    uses: ./.github/workflows/golang-ci.yaml
    with:
      working-directory: fai
