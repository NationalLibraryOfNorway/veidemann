name: Frontier

on:
  push:
    branches:
      - 'main'
    tags:
      - 'frontier-*'
    paths:
      - 'frontier/**'
  pull_request:
    paths:
      - 'frontier/**'
  workflow_dispatch:

jobs:
  release:
    if: github.event_name != 'pull_request'
    permissions:
      contents: read
      packages: write
    uses: ./.github/workflows/build-and-publish-java.yaml
    with:
      path: frontier