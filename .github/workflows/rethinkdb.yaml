name: RethinkDB

on:
  push:
    branches:
      - 'main'
    tags:
      - 'rethinkdb-*'
    paths:
      - 'rethinkdb/**'
  pull_request:
    paths:
      - 'rethinkdb/**'
  workflow_dispatch:

jobs:
  ci:
    uses: ./.github/workflows/golang-ci.yaml
    with:
      working-directory: rethinkdb/probe

  release:
    needs: ci
    if: github.event_name != 'pull_request'
    permissions:
      contents: read
      packages: write
    uses: ./.github/workflows/build-and-publish.yaml
    with:
      path: rethinkdb
