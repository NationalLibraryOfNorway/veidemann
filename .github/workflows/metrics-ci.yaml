name: Metrics CI

on:
  push:
    branches:
      - 'main'
    paths:
      - 'metrics/**'
  pull_request:
    paths:
      - 'metrics/**'
  workflow_dispatch:

jobs:
  ci:
    uses: ./.github/workflows/golang-ci.yaml
    with:
      working-directory: metrics
