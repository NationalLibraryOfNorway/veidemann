name: DB Initializer CI

on:
  push:
    branches:
      - 'main'
    paths:
      - 'rethinkdbadapter/**'
  pull_request:
    paths:
      - 'rethinkdbadapter/**'
  workflow_dispatch:

jobs:
  ci:
    uses: ./.github/workflows/java-ci.yaml
    with:
      subproject: rethinkdbadapter
