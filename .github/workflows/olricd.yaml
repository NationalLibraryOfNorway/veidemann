name: Olricd

on:
  push:
    branches:
      - 'main'
    tags:
      - 'olricd/*'
    paths:
      - 'olricd/**'

jobs:
  build:
    permissions:
      contents: read
      packages: write
    uses: ./.github/workflows/build-and-publish.yaml
    with:
      PATH: olricd
